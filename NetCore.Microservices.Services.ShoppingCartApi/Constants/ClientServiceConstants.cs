namespace NetCore.Microservices.Services.ShoppingCartApi.Constants;

public static class ClientServiceConstants
{
    public const string PRODUCT = nameof(PRODUCT);
    public const string COUPON = nameof(COUPON);
}